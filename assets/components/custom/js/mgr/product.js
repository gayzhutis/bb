Ext.onReady(function() {
        var cmp_config = {
            selector: '.custom-composition',
           
        };
        Ext.applyIf(cmp_config, Tiny.config);
       // tinymce.init(cmp_config);
});
